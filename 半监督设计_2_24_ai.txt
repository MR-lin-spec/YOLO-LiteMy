假设你是一名YOLO工程师，dataset.py包括了有标签和无标签类的定义方式，trainer.py是具体的训练逻辑，model.py是模型的具体定义
现在你想对这训练逻辑做半监督的调整
目前的data.yaml配置如下：

 voc.yaml
# 数据集根目录：当前所在目录 (~/autodl-tmp/YOLO2012)
# 使用 "." 代表当前目录，或者填写绝对路径 "/root/autodl-tmp/YOLO2012"
path: D:\YOLO_Project\VOCtrainval\VOCdevkit\YOLO2012

# 训练集和验证集路径 (相对于 path)
# 注意：请根据实际文件夹内部结构调整。
# 如果 train 文件夹下直接是图片 (xxx.jpg)，请将 "train/images" 改为 "train"
# 如果 train 文件夹下有 images 子文件夹 (train/images/xxx.jpg)，则保持 "train/images"
train: train\images  
val: val\images
# test: test/images  # 如果需要测试集

# 类别数量
nc: 20

# 类别名称 (必须全小写，与 transform.py 一致)
names:
  0: aeroplane
  1: bicycle
  2: boat
  3: bus
  4: car
  5: motorbike
  6: train
  7: bottle
  8: chair
  9: diningtable
  10: pottedplant
  11: sofa
  12: tvmonitor
  13: bird
  14: cat
  15: cow
  16: dog
  17: horse
  18: sheep
  19: person

请你完成：

1：对于YOLOUnlabeledDataset,请你给出适配的yaml文件，并在model.py的train方法中，允许同时处理有标签数据的配置文件和无标签数据的配置文件
2：在trainer.py中，创建self.unlabelset,并新增get_unlabeldataset方法，模仿self.trainset的处理方法，命名为self.unlabelloader
3：在trainer.py中，新增相应的get_unlabeldataloader方法，模仿get_dataloader方法
4：_setuo_train中，对原来的trainloader进行了batch处理，请你参考，新增对unlabeledloader的处理方法，得到的变量称为unlabelbatch
5：train方法中， 你已经获得了self.unlabelloader,请你修改下面这段代码
            pbar = TQDM(enumerate(self.train_loader), total=nb)  # 进度条显示
            self.tloss = None  # 总损失初始化
            for i, batch in pbar:
预期的for循环，代码是这样
for i,batch,unlabelbatch in pbar:
并且我可以通过unlabelbatch['img']获得无标签数据这一批次的图像

6:修改后我可以这样调用我的模型
model = YOLOLite("best_yolo26n.pt")  # 加载预训练模型权重
  # 如果有这个属性会显示yaml路径

# 不使用预训练模型，会导致损失难以下降
# model = YOLOLite("yololite3d/cfg/yolo11.yaml")
if __name__ == '__main__':
    # 如果是打包成 exe，需要这行
    # from multiprocessing import freeze_support
    # freeze_support()
    results = model.train(data="voc.yaml",unlabeldata="voc_unlabel.yaml" epochs=1, imgsz=640)

